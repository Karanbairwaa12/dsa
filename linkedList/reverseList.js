const { buildList } = require("./buildList")
const { printList } = require("./printList")

var reverse = function(head1) {
    let curr = head1;
    
    return curr;
}
let head1 = buildList([1,2,3,4,5,6])
printList(reverse(head1))
